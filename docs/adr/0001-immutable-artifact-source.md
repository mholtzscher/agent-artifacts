# Published artifact source is immutable

Published artifact source is immutable: every publish creates a new artifact, and later edits are limited to descriptive metadata such as title and description. This preserves stable public links, provenance, and historical feed integrity while still allowing small metadata corrections after publication.
